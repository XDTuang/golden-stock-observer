# 兜金隐含空间 · 小白使用与运维指南

> 适用对象：不熟悉命令行/自动化部署，但想自己验证数据、排查问题、看懂"每天自动更新"是怎么跑起来的同学。
> 涉及文件（都在仓库 `repurchase-yield/` 目录）：
> - `index.html` — 站点页面（含 13 条 7/1–7/20 真实样本 + DDDYB 密码门槛）
> - `update_repurchase.py` — 每日自动更新脚本（抓公告→算收益率→重写页面）
> - `records.json` — 数据仓储（跨日去重，自动更新时累加）
> - `.github/workflows/repurchase-daily.yml` — GitHub 定时机器人（每天北京 20:00 跑）

---

## 一、为什么你之前在主站看不到这个 tab（已修复）

上一轮我们只把页面生成在本地，**没有真正的 git push**，所以 GitHub 上自然什么都没有。
现在已执行 `git push origin main`（提交 `d50d0b4`），文件都在仓库里了。
GitHub Pages 会在推送后 **数十秒～几分钟** 内自动重建；若打开还是旧版/404，**硬刷新**即可（Mac：`Cmd+Shift+R`，Windows：`Ctrl+Shift+R`）。

主站入口：`https://xdtuang.github.io/golden-stock-observer/`
→ 登录主站（VIP 密码 `DDDYB`）→ 顶部导航点 **「📈 回购隐含收益」** → 内嵌页面自动放行（被 iframe 包裹时免二次输密），直接看数据。

---

## 二、本地验证 + 微调参数（`--dry-run` 实操）

脚本只依赖一个 Python 库 `requests`，你自己的 Mac 就够了，**不需要**那个很长的 managed 路径。

### 准备（只需做一次）
1. 打开「终端」：按 `Cmd+空格` 输入 `终端` 回车。
2. 进到项目目录（复制粘贴回车）：
   ```bash
   cd /Users/samt/golden_stock_observer/repurchase-yield
   ```
3. 装依赖（若已装过会秒过）：
   ```bash
   pip3 install requests
   ```

### 步骤 1：先跑"只看不写"的干跑
```bash
python3 update_repurchase.py --dry-run
```
你会看到类似输出：
```
[info] 数据窗口 2026-07-19 ~ 2026-07-20 (每日 20:00 触发)
[info] 巨潮命中含'回购'公告 N 条
[info] 可计算隐含收益率 M 条
[
  {"stock": "...", "code": "...", "target": ..., "close": ..., ...},
  ...
]
```
- **M > 0 且能看到真实股票名 + 目标价 + 收益率** → 链路通了，参数不用动。
- **N=0 / M=0 / 一堆 `[warn] HTTP 500`** → 巨潮把你的请求拦了，或参数要调（见下方"微调点"）。

### 步骤 2：回填历史区间测试（验证抓取+解析全链路）
```bash
python3 update_repurchase.py --from 2026-07-01 --to 2026-07-20 --dry-run
```
这会用 7/1–7/20 的窗口跑一遍，方便你确认能抓到那 13 条样本附近的数据。

### 微调点（脚本里改这几处，改完重新 `--dry-run`）
| 想调什么 | 在 `update_repurchase.py` 改哪行 | 说明 |
|---|---|---|
| 抓取源/接口 | `CNINFO_API = "https://www.cninfo.com.cn/..."` | 巨潮官方源；若被拦，可改用 `EM_API`（东方财富） |
| 关键词/筛选 | `fetch_cninfo_announcements` 里的 `"keyword": "回购"` 和 `if "回购" in title and any(...)` | 控制抓哪些公告 |
| 目标价解析正则 | `TARGET_RE = re.compile(r"...")` | 现在只从**标题**估价格；标题没写价格就跳过 |
| 请求头伪装 | `UA`、`http_get` 里的 `headers` | 浏览器 UA + Referer，降低被识破概率 |
| 随机延时 | `MIN_DELAY, MAX_DELAY = 0.6, 2.4` | 越大越"像人"，但越慢 |
| 重试次数 | `MAX_RETRY = 3` | 失败指数退避重试 |
| 代理池 | 运行前 `export REPO_PROXY=http://host:port` 或加 `--proxy http://host:port` | 数据中心 IP 被拦时的兜底 |

> ⚠️ 真实"回购目标价"必须**下钻公告正文（PDF/HTML）**解析，现在脚本只从标题估。
> 生产化前这是最后一个硬骨头（脚本 `parse_target_price` 处有 `TODO` 标注）。

---

## 三、真正的"每日自动抓取 + 反拦截 + 定时重生成"怎么落地

### 先说原理：为什么光有网页不行
浏览器打开的网页**不能**自己定时去抓数据、改文件——它得有个"后台机器人"按点干活。
这个机器人要么是一台常开服务器，要么是 GitHub 的免费定时任务（Actions）。
我们选了 **GitHub Actions**：免费、不用维护服务器、和仓库一体。

### 方案 A：GitHub Actions（已为你配好 ✅）
文件：`.github/workflows/repurchase-daily.yml`
- **触发时间**：每天 **北京 20:00**（cron `0 12 * * *`，UTC 12:00 = 北京 20:00）。
- **手动也能跑**：仓库页 → 顶部 **Actions** → 找「回购隐含收益 · 每日更新」→ 右侧 **Run workflow**。
- **它每天自动做**：
  1. 拉取仓库 → 装 Python + requests
  2. 跑 `update_repurchase.py`（抓前一日20:00→当日20:00的回购公告）
  3. 算隐含收益率、按 `(代码,公告日期)` 去重、累加进 `records.json`
  4. 重写 `index.html` 的 DATA 块
  5. 有变化就自动 `commit + push` → GitHub Pages 自动上线
- **反拦截在哪**：全在 `update_repurchase.py` 里（UA/Referer 头、随机延时、指数退避重试、可选代理）。机器人只是按时调用它。

#### 你怎么看它跑没跑 / 出没出错
1. 打开 `https://github.com/XDTuang/golden-stock-observer` → 点 **Actions** 标签。
2. 列表里每条是一次运行；点进去看日志（绿勾=成功，红叉=失败）。
3. 失败先看日志最后几十行：
   - `HTTP 500 / 连接超时` → 巨潮在拦机器人 IP（GitHub 服务器也是机房 IP）。
     解决办法：把脚本改成走 `EM_API`（东方财富）备选源，或加 `REPO_PROXY` 代理，或调大延时/重试。
   - `0 条` 但没报错 → 窗口内本就没新回购公告（正常，非交易日常见）。

### 方案 B：腾讯云（备选，节奏独立于主站）
仓库里已有 `腾讯云托管自动更新方案.md`，给出 COS+CDN / SCF 定时触发器 / 轻量服务器三条路线。
SCF 方案最贴合"每日20:00、且节奏独立于主站其他数据"——云函数定时触发器直接调这个脚本写 COS。
**优先把 GitHub Actions 跑顺**，腾讯云等你想换/加备份时再上。

### 你的日常运维清单（很轻）
- ✅ **平时不用管**：机器人每天 20:00 自己跑、自己上线。
- 🔧 **发现某天没新数据**：去 Actions 看日志，多半是巨潮拦了 → 按"微调点"改脚本 → `git push` 即可生效（下次自动用新脚本）。
- 🔧 **想立刻补跑一天**：本地 `--from/--to` 干跑确认无误后，也可以手动在 Actions 点 Run workflow（脚本默认抓昨日→今日）。
- 🔧 **数据明显不对**（如目标价错）：基本是"标题估价格"的局限 → 需补"公告正文解析"（见 TODO）。

---

## 四、一句话总结
- **看数据**：主站 → 回购隐含收益 tab（已 push，硬刷新即可见）。
- **本地验证/调参**：`cd repurchase-yield` → `pip3 install requests` → `python3 update_repurchase.py --dry-run`。
- **每日自动更新**：GitHub Actions 已配好，每天北京 20:00 自动抓+算+重写+上线，反拦截逻辑在脚本里，被拦了就去 Actions 看日志调参。
